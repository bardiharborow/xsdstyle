<?xml version="1.0" encoding="UTF-8" ?>
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema" targetNamespace="urn:notschema">
  <!-- The included document loads but its root is not xs:schema. -->
  <xs:include schemaLocation="notschema-target.xml" />
  <xs:element name="Root" type="xs:string" />
</xs:schema>
